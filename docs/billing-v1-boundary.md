# Billing v1 boundary

Implemented: organization plan catalog, subscription lifecycle fields, configurable overage rate, active employee usage calculation, activation capacity/status helpers, authorized subscription usage API, admin visibility, and regression tests.

Deferred: payment processor integration, checkout, invoices, automatic recurring charges, webhooks, receipts, tax handling, and provider-specific customer/payment objects.
